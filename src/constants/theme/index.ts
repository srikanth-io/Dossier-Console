import { colors } from "@/constants/theme/colors"
import { fonts, fontSizes, fontWeights } from "@/constants/theme/fonts"
import {
  componentHeights,
  iconSizes,
  layoutSizes,
  radii,
  spacing,
} from "@/constants/theme/sizes"

export const theme = {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  spacing,
  componentHeights,
  layoutSizes,
  iconSizes,
  radii,
} as const
